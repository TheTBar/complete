<% @body_id = 'product-details' %>

  <div data-hook="product_show" itemscope itemtype="https://schema.org/Product">
    <div class="col-md-5" data-hook="product_left_part">
      <div data-hook="product_left_part_wrap">
        <div id="product-images" data-hook="product_images">
          <div id="main-image" class="panel panel-default" data-hook>
            <div class="panel-body text-center">
              <%= render :partial => 'image' %>
            </div>
          </div>
          <div id="thumbnails" data-hook>
            <%= render :partial => 'thumbnails' %>
          </div>
        </div>

        <!-- <div data-hook="product_properties">
          <%#= render :partial => 'properties' %>
        </div>

        <div data-hook="promotions">
          <%#= render :partial => 'promotions' %>
        </div> -->
      </div>
    </div>

    <div class="col-md-7" data-hook="product_right_part">
      <div data-hook="product_right_part_wrap">
        <div id="product-description" data-hook="product_description">
          <div class="product-brand" itemprop="brand"><%= @product.brand %></div>
          <div class="product-title" itemprop="name"><%= @product.name %></div>

          <div class="well" itemprop="description" data-hook="description">
            <%= product_description(@product) rescue Spree.t(:product_has_no_description) %>
          </div>
          <div>
            <%= "Color: #{@product.color}" if !@product.color.nil? %>
          </div>

          <div id="cart-form" data-hook="cart_form">
            <%= render :partial => 'cart_form' %>
          </div>
        </div>

        <!-- look for similar items partial -->
        <%#= render :partial => 'taxons' %>
        <div class="additional-products-container">
        <% if !@additional_products.blank? %>
          <div id="taxon-crumbs" data-hook class=" five ">
            <h3 class="product-section-title">Other Items</h3>
                <% @additional_products.each do |additional_product| %>
                  <div class="additional_item_image"><%= link_to small_image(additional_product, itemprop: "image", class: "product-list-image"), spree.product_url(additional_product), itemprop: 'url', title: additional_product.name %></div>
                <% end %>
          </div>
        <% end %>
        </div>
      </div>
    </div>
  </div>
